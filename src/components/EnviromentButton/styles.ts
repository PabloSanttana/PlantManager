import styled from "styled-components/native";

interface ButtonProps {
  active?: boolean;
}

export const Button = styled.TouchableOpacity<ButtonProps>``;

export const ButtonTitles = styled.Text``;
